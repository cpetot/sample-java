name: Release and publish

on:
  push:
    branches:
      # - main
      - tech/essai-release

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Set up Java for publishing to Maven Central Repository
        uses: actions/setup-java@v3
        with:
          distribution: 'temurin'
          java-version: '11'
          cache: 'maven'
          server-id: ossrh
          server-username: MAVEN_USERNAME
          server-password: MAVEN_PASSWORD
          gpg-private-key: ${{ secrets.OSSRH_GPG_SECRET_KEY }}
          gpg-passphrase: MAVEN_GPG_PASSPHRASE

      - name: Increment to stable version
        run: |
          export currentSnapshotVersion=$(./mvnw help:evaluate -Dexpression=project.version -q -DforceStdout)
          echo "version=currentSnapshotVersion" >> $GITHUB_ENV
          export version=${{ env.version }}.split('-SNAPSHOT')[0]          
          echo "version=$version" >> $GITHUB_ENV
          
          ./mvnw build-helper:parse-version versions:set -DnewVersion=$version versions:commit
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add .
          git commit -m "Create stable version $version"
          git push

      - name: Create Git Tag
        run: |
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git tag -a $version -m "$version"
          git push origin $version

      - name: Build artifact
        run: mvn clean package -Prelease

      - name: Create release
        uses: ncipollo/release-action@v1
        with:
          allowUpdates: true
          artifacts: "${{ github.workspace }}/target/*.jar"
          token: ${{ secrets.GITHUB_TOKEN }}
          tag: "${{ env.version }}"

      - name: Publish to the Maven Central Repository
        run: |
          mvn \
            --no-transfer-progress \
            --batch-mode \
            deploy
        env:
          MAVEN_USERNAME: ${{ secrets.OSSRH_USERNAME }}
          MAVEN_PASSWORD: ${{ secrets.OSSRH_TOKEN }}
          MAVEN_GPG_PASSPHRASE: ${{ secrets.OSSRH_GPG_SECRET_KEY_PASSWORD }}

      - name: Create next SNAPSHOT
        run: |
          ./mvnw build-helper:parse-version versions:set -DnextSnapshot=true versions:commit
          export newSnapshotVersion=$(./mvnw help:evaluate -Dexpression=project.version -q -DforceStdout)
          
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add .
          git commit -m "Start new version $newSnapshotVersion"
          git push
